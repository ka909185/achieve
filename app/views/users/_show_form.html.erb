<div class="child row">
  <div class="wrapper col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">
    <div class="blog-text">
      <p class="left"><strong>ユーザ名:<%= @user.name %>さんの詳細</strong></p>

    </div>
    <div class="followed_users">
      <p><strong>フォローしているユーザ</strong></p>
      <% @followed_users.each do |f|%>
        <%= f.name %>
      <% end %>
    </div>
    <div class="followers">
      <p><strong>フォローされているユーザ</strong></p>
      <% @followers.each do |f|%>
        <%= f.name %>
      <% end %>
    </divs>
  </div>
</div>
